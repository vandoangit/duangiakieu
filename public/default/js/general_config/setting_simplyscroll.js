/*******************************************************************************
 
 Ghi chú:hoàn thành ngày 28-07-2015
 Copyright :Hồ Minh Trí
 
 ******************************************************************************/

$(window).load(function(){

	$("#marquee_view_product").simplyScroll({
		autoMode:'loop',
		orientation:'vertical'
	});

	$("#marquee_new_product").simplyScroll({
		autoMode:'loop',
		orientation:'vertical'
	});

	$("#marquee_buy_product").simplyScroll({
		autoMode:'loop',
		orientation:'vertical'
	});
});